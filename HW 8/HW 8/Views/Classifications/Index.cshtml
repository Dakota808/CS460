@model IEnumerable<HW_8.Models.Classification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artwork1.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Genre.genre1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artwork1.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Genre.genre1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.classification1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.classification1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.classification1 })
        </td>
    </tr>
}

</table>
